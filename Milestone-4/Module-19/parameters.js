function summation(a, b, c) {
    sum = a + b + c;
    console.log(sum);
}
summation(1, 2, 3);

function traingleArea(b, h) {
    let area = 1 / 2 * (b * h);
    console.log(area);
}
traingleArea(10, 15);